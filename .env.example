# Optional. Only needed if your API URL changes.
# The API URL is public, so it is safe here. Never put secrets in a VITE_ variable:
# everything starting with VITE_ is visible to anyone using the site.
VITE_API_URL=https://kcoze2d537.execute-api.ap-southeast-2.amazonaws.com/analyze
